properties(
    [buildDiscarder(
        logRotator(artifactDaysToKeepStr: '', 
        artifactNumToKeepStr: '', 
        daysToKeepStr: '14', 
        numToKeepStr: '10')
        ), 
    parameters(
        [credentials(
            credentialType: 'com.cloudbees.plugins.credentials.impl.UsernamePasswordCredentialsImpl', 
            defaultValue: 'Jenkins_Git_Account_ID', name: 'GIT_CREDENTIALS', required: false)]
        )
    ]
)